# --- Supabase ---
# URL du projet (Dashboard > Settings > API)
NEXT_PUBLIC_SUPABASE_URL=
# Clé anonyme (publique, protégée par RLS)
NEXT_PUBLIC_SUPABASE_ANON_KEY=
# Clé service role (SERVEUR UNIQUEMENT — jamais exposée au client)
SUPABASE_SERVICE_ROLE_KEY=

# --- Anthropic (Claude) ---
ANTHROPIC_API_KEY=
# Modèle utilisé pour le parsing et le matching (optionnel)
ANTHROPIC_MODEL=claude-opus-4-8

# --- Phase 7 uniquement (WhatsApp Cloud API) — ne pas remplir au MVP ---
# WHATSAPP_TOKEN=
# WHATSAPP_PHONE_NUMBER_ID=
# WHATSAPP_VERIFY_TOKEN=
